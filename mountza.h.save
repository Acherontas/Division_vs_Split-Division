#ifndef MOUNTZA_H
#define MOUNTZA_H
#include <vector>

class mountza
{
    public:
        mountza();
        virtual ~mountza();
        mountza(const mountza& other);
        mountza& operator=(const mountza& other);
        long double numbe;
        vector<long double> subseq;
        string o_mouzomenos;
        int mntz(long double fnum);

    protected:

    private:
};

#endif // MOUNTZA_H
